meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

build:
  requirementsPath: requirements.txt
  useCache: true

run:
scriptName: app.py
main: app.py
port: 8000
watchers:
  - path: .
    extensions: ['.py', '.env']
